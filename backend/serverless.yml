service: bus-backend

provider:
  name: aws
  runtime: nodejs16.x
  region: us-east-1
  profile: home
  versionFunctions: false
  logRetentionInDays: 30
  environment:
    BUS_TABLE: !ImportValue ExtPennyTableName-${self:custom.environment}
custom:
  stage: ${opt:stage, self:provider.stage}
  environments:
    dev: dev
    prod: prod
  environment: ${self:custom.environments.${self:custom.stage}, self:custom.environments.dev}
  esbuild:
    bundle: true
    minify: true
    exclude:
      - "*"
    external:
      - next

package:
  patterns:
    - "!./**"
    - "node_modules"
    - "package.json"

plugins:
  - serverless-esbuild
  - serverless-offline
functions:
  Create:
    handler: ./Create.handler
    role: DBLambdaRole
    events:
      - http:
          method: POST
          path: api/{type}/{slug}
          cors: true
  Get:
    handler: ./Get.handler
    role: DBLambdaRole
    events:
      - http:
          method: GET
          path: api/{type}/{slug}
          cors: true
  GetAll:
    handler: ./GetAll.handler
    role: DBLambdaRole
    events:
      - http:
          method: GET
          path: api/{type}
          cors: true
  Update:
    handler: ./Update.handler
    role: DBLambdaRole
    events:
      - http:
          method: PUT
          path: api/update
          cors: true
  Delete:
    handler: ./Delete.handler
    role: DBLambdaRole
    events:
      - http:
          method: DELETE
          path: api/{type}/{slug}
          cors: true

resources:
  Resources:
    DBLambdaRole:
      Type: "AWS::IAM::Role"
      Properties:
        RoleName: DBLambdaRole-${self:custom.stage}
        AssumeRolePolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: "Allow"
              Principal:
                Service:
                  - lambda.amazonaws.com
                  - edgelambda.amazonaws.com
              Action:
                - sts:AssumeRole
        Policies:
          - PolicyName: "LambdaRole-Policy"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action:
                    - logs:*
                  Resource: arn:aws:logs:*:*:*

          - PolicyName: "PutBusSiteDB"
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: "Allow"
                  Action:
                    - "dynamodb:GetItem"
                    - "dynamodb:Query"
                    - "dynamodb:PutItem"
                    - "dynamodb:UpdateItem"
                  Resource:
                    "*"
                    # - arn:aws:s3:::${self:custom.staticAssetBucket}/db/*
                    # - arn:aws:s3:::${self:custom.staticAssetBucket}/db/
  Outputs:
    PennyBackendAPI:
      Value:
        Ref: ApiGatewayRestApi
      Export:
        Name: ExtPennyBackendAPI-${self:custom.stage}
